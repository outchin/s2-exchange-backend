{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "python manage.py collectstatic --noinput && daphne -b 0.0.0.0 -p $PORT s2exchange_api.asgi:application",
    "preDeployCommand": "python manage.py migrate --noinput && python manage.py create_default_superuser && python manage.py seed_exchange_data",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
